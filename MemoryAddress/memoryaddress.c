#include <stdio.h>

int main()
{
    int myAge = 43;
    printf("%p", &myAge); // Outputs 0x... (Hexadecimal format)

    // Hexadecimal -> can be variant depends on where the variable is stored in our computer
}